# FastX-Tui 示例插件

这是一个 FastX-Tui 插件系统的示例插件，展示了插件的基本功能和最佳实践。

## 📦 插件信息

- **名称**: 示例插件
- **版本**: 1.0.0
- **作者**: FastX Team
- **分类**: 开发
- **标签**: 示例、开发、模板
- **许可证**: MIT
- **仓库**: [https://github.com/fastxteam/FastX-Tui-Plugin-Example](https://github.com/fastxteam/FastX-Tui-Plugin-Example)

## 🎯 功能特性

- 展示插件的基本结构和功能
- 演示如何创建插件子菜单
- 演示如何向主菜单添加命令
- 演示如何向现有子菜单添加命令
- 演示如何访问插件资源文件
- 演示如何使用系统提供的日志接口
- 演示如何定义插件信息

## 📋 安装方法

### 方法一：通过插件管理器在线安装

1. 打开 FastX-Tui 应用
2. 进入插件管理界面
3. 点击 "在线安装"
4. 搜索 "FastX-Tui-Plugin-Example"
5. 点击 "安装" 按钮

### 方法二：手动安装

1. 克隆插件仓库到 plugins 目录：
   ```bash
   git clone https://github.com/fastxteam/FastX-Tui-Plugin-Example.git plugins/FastX-Tui-Plugin-Example
   ```

2. 重启 FastX-Tui 应用，或在插件管理界面点击 "重载所有插件"

## 🚀 使用说明

### 插件菜单

安装完成后，插件会在 FastX-Tui 应用中创建一个 "示例插件菜单"，包含以下命令：

- **插件问候**: 显示插件的问候信息
- **插件信息**: 显示插件的详细信息
- **资源示例**: 展示如何访问插件资源文件

### 主菜单命令

插件还会向主菜单添加一个 "主菜单示例命令"，可以直接在主菜单中使用。

### 系统工具菜单命令

插件会尝试向系统工具菜单添加一个 "系统工具示例命令"。

## 📁 插件结构

```
FastX-Tui-Plugin-Example/
├── fastx_plugin.py          # 插件入口文件（必须）
├── pyproject.toml           # 插件元数据和依赖声明
├── README.md                # 插件说明文档
├── LICENSE                  # 许可证文件
├── resources/               # 插件资源文件目录
│   └── example.txt          # 示例资源文件
└── bin/                     # 二进制文件目录（可选）
    └── example_binary       # 示例二进制文件（可选）
```

## 🛠️ 开发说明

### 插件入口文件

插件的入口文件必须命名为 `fastx_plugin.py`，并且包含一个继承自 `Plugin` 类的插件类。

### 插件类结构

```python
from core.plugin_manager import Plugin, PluginInfo

class ExamplePlugin(Plugin):
    def get_info(self) -> PluginInfo:
        # 返回插件信息
        pass
    
    def initialize(self):
        # 初始化插件
        pass
    
    def cleanup(self):
        # 清理插件资源
        pass
    
    def register(self, menu_system):
        # 注册插件命令
        pass
```

### 系统提供的接口

插件可以使用系统提供的以下接口：

- **日志接口**: `self.log_debug()`, `self.log_info()`, `self.log_warning()`, `self.log_error()`, `self.log_critical()`
- **资源文件接口**: `self.get_resource_path(resource_name)`
- **二进制文件接口**: `self.get_binary_path()`
- **插件路径**: `self.plugin_path`

## 📝 最佳实践

1. **安全第一**: 插件的所有操作都应该使用 try-except 包装，避免因插件错误导致整个系统崩溃
2. **资源管理**: 及时清理插件使用的资源，如关闭文件句柄、数据库连接等
3. **日志记录**: 合理使用日志记录插件的运行状态和错误信息
4. **依赖管理**: 在 pyproject.toml 中声明插件的依赖，避免使用系统不支持的依赖
5. **兼容性**: 在 PluginInfo 中声明插件的兼容性要求，如 fastx-tui 版本要求
6. **文档完善**: 提供详细的 README.md 文件，说明插件的功能、安装方法和使用说明

## 📄 许可证

本插件使用 MIT 许可证，详见 [LICENSE](LICENSE) 文件。

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📞 联系方式

- **作者**: FastX Team
- **邮箱**: team@fastx-tui.com
- **仓库**: [https://github.com/fastxteam/FastX-Tui-Plugin-Example](https://github.com/fastxteam/FastX-Tui-Plugin-Example)
